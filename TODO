Make type for all objects data

typedef struct objects_data
{
  O_objects objects[O_NB_MAX_OBJECTS];
  float objects_prop[O_NB_MAX_OBJECTS];
  float objects_dist[O_NB_MAX_OBJECTS];
  int nb_oject;
} Objects_data;
